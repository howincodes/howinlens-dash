import Referrers from '@/pages/sales/Referrers'

// The /reports/sales/referrer-leaderboard page reuses the Referrers component.
// Kept as a separate route so it lives under the Reports hub navigation too.
export default function SalesReferrerReport() {
  return <Referrers />
}
